{% extends 'base.html.twig' %}

{% block title %}{{ note.title }}{% endblock %}

{% block body %}
    <a href="{{ path('app_note_index') }}">Zpět na seznam poznámek</a>
    <a href="{{ path('app_note_edit', {'id': note.id}) }}">Upravit</a>

    <h1>{{ note.title }}</h1>

    {{ note.content|markdown_to_html }}
{% endblock %}
