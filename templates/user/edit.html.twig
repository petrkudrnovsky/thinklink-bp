{% extends 'base.html.twig' %}

{% block title %}Upravit uživatele{% endblock %}

{% block body %}
    <h1>Upravit uživatele: {{ user.name }}</h1>

    {{ include('user/_form.html.twig', {'button_label': 'Aktualizovat'}) }}

    {% if is_granted('ROLE_ADMIN') %}
        <a href="{{ path('app_user_index') }}">Zpět na seznam uživatelů</a>
        {{ include('user/_delete_form.html.twig') }}
    {% endif %}
{% endblock %}
